# Controller
# Note: For Docker-first usage, database is persisted in Docker volume.
# This path is used for local CLI tools and testing only.
AUTOSERVE_DB_PATH=/tmp/autoserve/autoscaler.db

# Nginx 
AUTOSERVE_NGINX_CONTAINER=autoserve-nginx
AUTOSERVE_NGINX_CONF_DIR=./configs/nginx

# Controller host and port settings
AUTOSERVE_HOST=0.0.0.0
AUTOSERVE_PORT=8000


# Optional
# Logging level (DEBUG, INFO, WARNING, ERROR, CRITICAL)
# LOG_LEVEL=INFO

# Docker socket path (if different from default)
# DOCKER_SOCKET=/var/run/docker.sock

# Network name for containers
# AUTOSERVE_NETWORK=autoserve

# SSL/TLS Configuration (if using HTTPS)
# AUTOSERVE_SSL_CERT_PATH=/path/to/cert.pem
# AUTOSERVE_SSL_KEY_PATH=/path/to/key.pem

# Metrics configuration
# AUTOSERVE_METRICS_ENABLED=true
# AUTOSERVE_METRICS_PORT=9090

# Health check configuration
# AUTOSERVE_HEALTH_CHECK_INTERVAL=30
# AUTOSERVE_HEALTH_CHECK_TIMEOUT=10

# Scaling configuration defaults
# AUTOSERVE_DEFAULT_MIN_REPLICAS=1
# AUTOSERVE_DEFAULT_MAX_REPLICAS=10
# AUTOSERVE_DEFAULT_TARGET_CPU=70
# AUTOSERVE_DEFAULT_TARGET_MEMORY=80

# Authentication (if implementing API authentication)
# AUTOSERVE_API_KEY=your-secret-api-key
# AUTOSERVE_AUTH_ENABLED=false
